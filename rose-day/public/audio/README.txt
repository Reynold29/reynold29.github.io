Background music for the Valentine site

Place your audio file here and name it:

  background.mp3

Supported formats: MP3 is most reliable. You can also use .ogg or .m4a
if you add a second <source> in the app (see BackgroundMusic.tsx).

The track will loop and play by default; users can mute/unmute with the
button in the bottom-right corner.
